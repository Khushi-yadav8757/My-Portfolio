<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khushi's Portfolio</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            cursor: none;
        }

        body {
            font-family: 'Poppins', sans-serif;
            line-height: 1.6;
            background: #0a0a0a;
            color: #fff;
            overflow-x: hidden;
        }

        .cursor {
            width: 20px;
            height: 20px;
            border: 2px solid #00ff88;
            border-radius: 50%;
            position: fixed;
            pointer-events: none;
            z-index: 9999;
            transition: 0.1s;
            mix-blend-mode: difference;
        }

        .cursor.grow {
            transform: scale(2);
            background: rgba(0, 255, 136, 0.3);
            mix-blend-mode: normal;
        }

        nav {
            position: fixed;
            width: 100%;
            padding: 20px;
            background: rgba(0, 0, 0, 0.95);
            backdrop-filter: blur(10px);
            z-index: 100;
        }

        nav ul {
            display: flex;
            justify-content: flex-end;
            list-style: none;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        nav ul li {
            margin-left: 40px;
        }

        nav ul li a {
            color: #fff;
            text-decoration: none;
            font-size: 18px;
            transition: 0.3s;
            position: relative;
            padding: 5px 0;
        }

        nav ul li a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: #00ff88;
            transition: 0.3s;
        }

        nav ul li a:hover::after {
            width: 100%;
        }

        section {
            min-height: 100vh;
            padding: 100px 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .hero {
            background: linear-gradient(135deg, #000, #111);
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent 45%, rgba(0, 255, 136, 0.1) 50%, transparent 55%);
            animation: shine 10s infinite linear;
        }

        @keyframes shine {
            0% { transform: translateX(-50%) translateY(-50%) rotate(0deg); }
            100% { transform: translateX(-50%) translateY(-50%) rotate(360deg); }
        }

        .hero h1 {
            font-size: 4.5em;
            margin-bottom: 20px;
            background: linear-gradient(45deg, #00ff88, #00ffea);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: glow 2s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from { text-shadow: 0 0 10px #00ff88, 0 0 20px #00ff88; }
            to { text-shadow: 0 0 20px #00ffea, 0 0 30px #00ffea; }
        }

        .hero p {
            font-size: 1.8em;
            color: #888;
            margin-bottom: 30px;
        }

        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            padding: 40px;
        }

        .skill-card {
            background: rgba(255, 255, 255, 0.05);
            padding: 30px;
            border-radius: 15px;
            text-align: center;
            transition: 0.3s;
            position: relative;
            overflow: hidden;
        }

        .skill-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, transparent, rgba(0, 255, 136, 0.1), transparent);
            transform: translateX(-100%);
            transition: 0.5s;
        }

        .skill-card:hover::before {
            transform: translateX(100%);
        }

        .skill-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 30px rgba(0, 255, 136, 0.2);
        }

        .skill-card h3 {
            font-size: 24px;
            margin-bottom: 15px;
            color: #00ff88;
        }

        .skill-card p {
            font-size: 16px;
            color: #888;
        }

        .skill-level {
            width: 100%;
            height: 6px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 3px;
            margin-top: 20px;
            overflow: hidden;
        }

        .skill-progress {
            height: 100%;
            background: #00ff88;
            width: 0;
            transition: width 1s ease-in-out;
        }

        .contact {
            text-align: center;
        }

        .contact-info {
            margin: 20px 0;
            padding: 30px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 15px;
            transition: 0.3s;
            backdrop-filter: blur(10px);
        }

        .contact-info:hover {
            transform: scale(1.05);
            box-shadow: 0 10px 30px rgba(0, 255, 136, 0.2);
        }

        h2 {
            font-size: 3em;
            margin-bottom: 40px;
            color: #00ff88;
            text-align: center;
        }

        @media (max-width: 768px) {
            .hero h1 { font-size: 3em; }
            .hero p { font-size: 1.2em; }
            nav ul { justify-content: center; }
            nav ul li { margin: 0 15px; }
        }
    </style>
</head>
<body>
    <div class="cursor"></div>

    <nav>
        <ul>
            <li><a href="#home">Home</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#skills">Skills</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
    </nav>

    <section id="home" class="hero">
        <div class="container">
            <h1>Khushi Kumari Yadav</h1>
            <p>Aspiring Data Engineer | B.Tech Student</p>
        </div>
    </section>

    <section id="about">
        <div class="container">
            <h2>About Me</h2>
            <p>I'm a second-year B.Tech student at KCC Institute of Technology with a passion for data engineering. My goal is to leverage technology to transform raw data into meaningful insights.</p>
        </div>
    </section>

    <section id="skills">
        <div class="container">
            <h2>Skills</h2>
            <div class="skills-grid">
                <div class="skill-card">
                    <h3>C</h3>
                    <p>Strong foundation in C programming with experience in data structures and algorithms</p>
                    <div class="skill-level">
                        <div class="skill-progress" data-level="85"></div>
                    </div>
                </div>
                <div class="skill-card">
                    <h3>C++</h3>
                    <p>Proficient in object-oriented programming and STL</p>
                    <div class="skill-level">
                        <div class="skill-progress" data-level="80"></div>
                    </div>
                </div>
                <div class="skill-card">
                    <h3>Python</h3>
                    <p>Experience with data analysis and manipulation libraries</p>
                    <div class="skill-level">
                        <div class="skill-progress" data-level="90"></div>
                    </div>
                </div>
                <div class="skill-card">
                    <h3>Java</h3>
                    <p>Knowledge of core Java concepts and application development</p>
                    <div class="skill-level">
                        <div class="skill-progress" data-level="75"></div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="contact">
        <div class="container">
            <h2>Contact Me</h2>
            <div class="contact-info">
                <p>📧 khushishresth5@gmail.com</p>
            </div>
            <div class="contact-info">
                <p>📱 8757595152</p>
            </div>
        </div>
    </section>

    <script>
        const cursor = document.querySelector('.cursor');
        const links = document.querySelectorAll('a');
        const cards = document.querySelectorAll('.skill-card, .contact-info');

        // Custom cursor
        document.addEventListener('mousemove', (e) => {
            cursor.style.left = e.clientX + 'px';
            cursor.style.top = e.clientY + 'px';
        });

        [...links, ...cards].forEach(element => {
            element.addEventListener('mouseenter', () => cursor.classList.add('grow'));
            element.addEventListener('mouseleave', () => cursor.classList.remove('grow'));
        });

        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });

        // Animate skill progress bars
        function animateSkills() {
            document.querySelectorAll('.skill-progress').forEach(progress => {
                const level = progress.getAttribute('data-level');
                progress.style.width = level + '%';
            });
        }

        // Animate skills when they come into view
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    animateSkills();
                }
            });
        });

        observer.observe(document.querySelector('.skills-grid'));
    </script>
</body>
</html>
